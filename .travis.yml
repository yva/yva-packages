language: node_js
node_js:
  - "node"
install:
  - npx lerna bootstrap --hoist
script:
  - npm run build
  - npm test -- --coverage
after_success:
  - bash <(curl -s https://codecov.io/bash)
deploy:
  provider: script
  skip_cleanup: true
  script:
    - npm run deploy
branches:
  only:
    - master
    - next
cache:
  directories:
    - "$HOME/.npm"
notifications:
  email: false
